# --- Option A: plain HTTP (local / behind another load balancer) ---
:80 {
	reverse_proxy ultar:3000
}

# --- Option B: automatic HTTPS for a public domain ---
# Uncomment the block below and remove the :80 block above.
#
# your.domain.com {
# 	reverse_proxy ultar:3000
# }
