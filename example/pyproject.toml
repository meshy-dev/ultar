[project]
name = "ultar-examples"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "datasets>=2.19",
    "hf-xet>=1.1",
    "huggingface-hub[hf-xet]>=0.23",
]

[tool.uv]
package = false
